# NipponRisk Analytics — backend environment (copy to .env to override)
# Data freshness TTL in seconds (default 1 day)
NIPPORISK_CACHE_TTL=86400

# Optional: force a longer/shorter yfinance lookback in the fetch script
# NIPPORISK_PERIOD=5y