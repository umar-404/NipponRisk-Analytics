{
  "name": "nipponrisk-analytics",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "npm install --prefix frontend && npm run build --prefix frontend",
    "data:export": "./backend/.venv/bin/python scripts/export_worker_data.py",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "npm run typecheck --prefix frontend && tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5",
    "typescript": "^5.5.3",
    "wrangler": "^4"
  }
}